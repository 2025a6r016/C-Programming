#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

  double base,exponent,result;
    scanf("%lf %lf",&base,&exponent);
    result = pow(base , exponent);
    printf("%.0lf\n",result);
    return 0;
}
